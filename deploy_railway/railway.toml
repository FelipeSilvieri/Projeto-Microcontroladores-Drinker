[build]
builder = "nixpacks"
buildCommand = "pip install -r requirements.txt --target /workspace"

[deploy]
startCommand = "gunicorn index:app"
healthcheckPath = "/getalldrinks"
healthcheckTimeout = 100
restartPolicyType = "Never"
